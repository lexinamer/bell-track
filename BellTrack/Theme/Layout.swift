import Foundation

struct Layout {

    // Spacing between items inside a card (rows, text blocks)
    static let cardSpacing: CGFloat = Spacing.sm   // 8

    // Spacing between content elements (label → field, lines)
    static let contentSpacing: CGFloat = Spacing.sm // 8

    // Spacing between list rows / cards
    static let listSpacing: CGFloat = Spacing.md   // 16

    // Spacing between major sections / blocks
    static let sectionSpacing: CGFloat = Spacing.lg // 24
    
    // Horizontal spacing
    static let horizontalSpacing: CGFloat = Spacing.lg // 24
    static let horizontalSpacingNarrow: CGFloat = Spacing.md // 16
}
